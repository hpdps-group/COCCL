schema_version = 3

[runtime]
mode = "normal"
compression_threshold_bytes = 0

[compressor_plugins]
compressors = ["sdp4bit"]
library_path = "../../../build/obj/coccl-extend/compressor_plugin/libcompress"

[normal.sendrecv.default]
compressor = "sdp4bit"

[normal.sendrecv.default.config]
groupCount = 128
quantBits = 4
quantType = "Symmetric"
