fp8_format: E4M3       # 可选 E4M3 或 E5M2
saturation: 1          # 0 = no saturation, 1 = saturate finite
use_scale: 1           # 0 = 不使用 scale, 1 = 使用 scale
group_size: 128        # 分组大小 (仅当 use_scale=1 时有效)
target_range: 448.0    # E4M3 最大 448, E5M2 最大 57344
