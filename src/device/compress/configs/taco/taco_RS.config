fp8_format: 0          # 0=E4M3, 1=E5M2
saturation: 1            # 1=启用饱和
use_scale: 1           # 1=启用缩放
group_size: 128        # 分组大小
target_range: 448.0    # 目标范围
safe_mode: 0           # 0=禁用安全模式
# Hadamard变换
use_hadamard: 1            # 1=启用Hadamard变换
pivotSwap: 0